{
  "name": "fluxforge-site",
  "version": "1.0.0",
  "private": true,
  "description": "FluxForge production static website (HTML + Tailwind CSS + vanilla JS), published to GitHub Pages at fluxforge.pt.",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && node scripts/dev-server.js",
    "check": "node scripts/check.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
